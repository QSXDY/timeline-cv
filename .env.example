# ============================================================
# timeline-cv 部署配置模板（复制为 .env 后按需修改）
# .env 不入库（真实配置不要提交到 GitHub）
# ============================================================

# 生产模式：Docker Hub 镜像地址
# 不设置时默认 cnqsxdy/timeline-cv:latest（docker compose pull && up -d 直接可用）
# 固定版本部署示例：RESUME_IMAGE=cnqsxdy/timeline-cv:1.0.0
# 本地开发：RESUME_IMAGE=timeline-cv:local（配 docker compose up -d --build）
RESUME_IMAGE=cnqsxdy/timeline-cv:latest
