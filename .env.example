# ============================================================
# yfq-resume 部署配置模板（复制为 .env 后按需修改）
# .env 不入库（真实配置不要提交到 GitHub）
# ============================================================

# 生产模式：Docker Hub 镜像地址（别人部署用这个）
# 格式：<你的DockerHub用户名>/timeline-cv:<版本号>
# 示例：RESUME_IMAGE=qsxdy/timeline-cv:1.0.0
# 升级：改版本号 -> docker compose pull && docker compose up -d
RESUME_IMAGE=
